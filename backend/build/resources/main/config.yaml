# 服务器配置
server:
  host: "0.0.0.0"
  port: 8080

# 数据库配置
database:
  # 类型: sqlite, mysql
  type: "sqlite"
  # SQLite 配置（type 为 sqlite 时使用）
  path: "./data/guet_map.db"
  # MySQL 配置（type 为 mysql 时使用）
  host: "localhost"
  port: 3306
  username: "root"
  password: "123456"
  name: "guet_map"

# Redis 配置（用于验证码缓存，可选）
redis:
  enabled: false
  host: "localhost"
  port: 6379
  password: ""
  database: 0
  code-prefix: "guet_map:code:"
  code-expire-seconds: 600

# JWT配置
jwt:
  secret: "your-super-secret-jwt-key-change-in-production-guet-map-2024"
  expiration-hours: 720  # 30天

# 邮件配置 (请填写您的邮箱信息)
mail:
  enabled: true
  host: "smtp.qq.com"
  port: 465
  use-tls: false
  use-ssl: true
  username: "2793553195@qq.com"      # 替换为您的QQ邮箱
  password: "lyitfxqsbjerdffg"       # 替换为QQ邮箱授权码
  from: "GUET Map <2793553195@qq.com>"

# CORS配置
cors:
  allowed-origins:
    - "*"
  allowed-methods:
    - GET
    - POST
    - PUT
    - DELETE
    - OPTIONS
  allowed-headers:
    - "*"
